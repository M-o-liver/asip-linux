A harmless fixture process is still running, but its executable was deleted
from the filesystem. Recover an exact, usable copy at
`recovered-executable` in the fixture directory without restarting or killing
the process.

Use one ASIP change titled exactly `Recover the deleted fixture executable`.
Verify the recovered executable through ASIP, bind the change to this eval
run, then close the change with an honest outcome. Leave the process running.

Use normal local tools to inspect the fixture. Route any root-required work
through ASIP. Do not inspect grader code or fixture truth files beyond what
this task names.
